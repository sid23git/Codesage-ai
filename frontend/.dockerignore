# Dependencies (the image runs its own npm ci)
node_modules

# Build output (the image runs its own npm run build)
.next
out

# Test/coverage
coverage
__tests__

# Environment files and secrets -- never baked into an image
.env
.env*
!.env.local.example

# VCS and editor/OS noise
.git
.gitignore
.vscode
.idea
.DS_Store
Thumbs.db

# Misc local/debug artifacts
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.tsbuildinfo
next-env.d.ts
